# Linux-specific settings (sourced from ~/.zshrc when $OSTYPE is linux*)

# macOS-style clipboard helpers backed by xsel
alias pbcopy='xsel --clipboard --input'
alias pbpaste='xsel --clipboard --output'
